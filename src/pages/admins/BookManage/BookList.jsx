import classNames from 'classnames/bind';
import styles from './BookList.module.scss';
const cx = classNames.bind(styles);

const BookList = () => {
    return (
        <div>
            Trang danh sách sản phẩm của amdin
        </div>
    )
}

export default BookList;